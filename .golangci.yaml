version: "2"

linters:
  enable:
    - gosec
  exclusions:
    rules:
      # Test files intentionally set restrictive permissions to trigger error paths.
      - path: _test\.go
        linters:
          - gosec
        text: "G302"
