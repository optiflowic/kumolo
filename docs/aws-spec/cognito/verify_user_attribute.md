# VerifyUserAttribute

See [GetUserAttributeVerificationCode / VerifyUserAttribute](get_user_attribute_verification_code.md)
— the two operations share a single memo since they form one flow (request a
code, then submit it).
