services:
  kumolo:
    build: .
    ports:
      - "5566:5566"
    environment:
      KUMOLO_PORT: "5566"
      KUMOLO_LOG_LEVEL: "debug"
      # Uncomment to persist data across container restarts:
      # KUMOLO_DATA_DIR: "/data"
    # volumes:
    #   - ./data:/data
